---
# Infra suite - orchestrates all infrastructure tasks
- name: Infra - General infrastructure packages
  include_tasks: general.yml

# Additional infrastructure configuration files can be added as needed:
# - kubernetes.yml (Kubernetes tools)
# - terraform.yml (Terraform setup)
# - cloud-tools.yml (Cloud CLI tools)