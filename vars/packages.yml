# Package definitions by category
core_packages:
  - curl
  - wget
  - git
  - git-crypt
  - build-essential
  - ansible
  - just

sys_packages:
  - htop
  - tree
  - unzip
  - zip

shell_packages:
  - tmux

dev_packages:
  - nodejs
  - python3-pip
  - golang-go

infra_packages:
  - ansible

offcoms_packages:
  - firefox
  - thunderbird